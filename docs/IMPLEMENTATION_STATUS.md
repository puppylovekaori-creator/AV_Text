# AV_Text 実装状況

## 2026-06-12
- 右クリックメニューに `女優に追加` を追加済み
- 右クリックメニューに `女優に別名で追加` を追加済み
- Native host に `actress_append` / `actress_append_with_alias` target を追加済み
- 追記時は既存内容を保持し、必要時のみ半角スペース 1 個を挿入する実装へ変更済み
- アドオン version を `1.9` へ更新済み
- Firefox 再配布用 zip を `avtext-ffext1.9.zip` として出力対象にする
